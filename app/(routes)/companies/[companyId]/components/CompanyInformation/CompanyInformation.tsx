import { CompanyInformationProps } from "./CompanyInformation.type";

export function CompanyInformation(props: CompanyInformationProps) {
  const { company } = props;
  return <div>Companies informacion</div>;
}
